<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SMSNode" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    
    <style name="Theme.SMSNode.Splash" parent="Theme.SplashScreen">
        <!-- Background color -->
        <item name="windowSplashScreenBackground">@color/white</item>

        <!-- SVG Logo -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_forwardsms</item>

        <!-- Optional -->
        <item name="postSplashScreenTheme">@style/Theme.SMSNode</item>
    </style>

    <style name="Theme.SMSNode" parent="Base.Theme.SMSNode" />
</resources>